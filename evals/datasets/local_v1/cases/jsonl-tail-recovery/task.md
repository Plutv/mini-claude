# 修复JSONL崩溃恢复

`app/session_store.py`读取会话JSONL。进程在追加最后一行时崩溃可能留下截断尾行，目前会导致整个会话无法恢复。

要求：

- 忽略空行。
- 最后一个非空行损坏时，保留并返回此前所有合法记录。
- 中间行损坏必须抛出`json.JSONDecodeError`，不能静默丢失后续历史。
- 正确保留Unicode内容和原有记录顺序。
- 不修改文件内容，不新增第三方依赖。
