# 修复多来源配置合并

`app/config.py`会把默认值、TOML、`.env`和进程环境变量合并为最终配置，但当前优先级错误。

要求：

- 优先级必须是：进程环境变量 > `.env` > TOML > 默认值。
- 值为`None`表示该来源没有设置，不得覆盖低优先级值。
- 不得修改调用方传入的字典或`DEFAULTS`。
- 保持现有函数签名，不新增第三方依赖。
- 运行测试并确保全部通过。
