# 修复异步Pending请求管理

`app/pending.py`使用全局注册表跟踪异步请求，但超时、取消和重复请求会造成状态覆盖或内存泄漏。

要求：

- 同一`key`已有活跃请求时抛出`DuplicateRequestError`。
- 请求成功、异常、超时或调用方取消后，注册表都必须清理。
- 超时或调用方取消时，底层Task不能继续在后台运行。
- 取消必须继续向调用方传播，不能被转换为普通结果。
- 保持现有公开API，不新增第三方依赖。
