# 修复重试策略

`app/retry.py`会对所有异常和所有操作自动重试，可能重复执行写文件、扣款等副作用操作。

要求：

- 只有`TransientError`可以触发自动重试。
- 只有`idempotent=True`时允许重试；非幂等操作第一次失败后立即抛出。
- `attempts`表示总尝试次数，必须至少为1。
- 重试等待采用1、2、4……秒指数退避，并继续使用注入的`sleep`函数以便测试。
- 普通异常和取消不能被吞掉或重试。
- 成功时立即返回，不执行多余等待。
