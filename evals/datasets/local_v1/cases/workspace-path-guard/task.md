# 修复工作区路径校验

`app/workspace.py`用于限制Agent只能访问指定工作区，但当前字符串前缀判断可以被`..`、绝对路径和同名前缀目录绕过。

要求：

- 返回规范化后的绝对`Path`。
- 允许工作区内部文件以及工作区根目录本身。
- 拒绝`..`逃逸、工作区外绝对路径和类似`workspace-other`的同名前缀目录。
- 解析符号链接后的目标也必须位于工作区内。
- 越界统一抛出`WorkspaceEscapeError`。
- 不通过简单字符串`startswith`实现。
